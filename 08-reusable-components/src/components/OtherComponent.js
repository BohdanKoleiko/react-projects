const OtherComponent = function () {
   return (
      <div>
         <p>This is just a simple text that maybe should describe h1 title but it doesn't</p>
      </div>
   );
};

export default OtherComponent;
